# mymodule.py

def greet(name):
    return f"Hello, {name}!"

print("This code runs automatically when the module is imported.")

if __name__ == "__main__":
    print("This code runs only when the module is executed directly. (from the sample library!)")
